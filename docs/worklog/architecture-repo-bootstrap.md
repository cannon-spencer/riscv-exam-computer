# Architecture, repo, and ecosystem map

**EEL4907 · Hardened RISC-V Exam Computer**

**Accomplishment:** We locked **what we are building** into **repo structure** and **reports**: **R2 design draft** (UI flow login → Duo → Canvas → integrity), **network/hardware figures**, `**exam-env/`** and `**platform/reptilian-riscv/**` story, and LaTeX under `**docs/latex/**`—plus a clear map of **official SEB** vs `**seb-linux`**, **Cage** as kiosk compositor direction, and **browser stack reality on riscv64** (survey → **WebKitGTK** path for `seb-linux`).

**When:** **Mar 3 – Mar 28, 2026** (heaviest doc/repo push; overlaps M2 integration after mid-March).

Corroboration: **commits** on `main`, **report PDFs**, **Jira** where design tasks were tracked.