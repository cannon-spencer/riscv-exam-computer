# Changelog — reptilian-riscv (Handed-Off OS)
 
Tracks changes made on top of the original handed-off version.
The submodule itself stays untouched. Log any changes here.

NOTE: BananaPi development ended, but 1 needed change was discovered. Drop `ctype.h` directly into `bananapi/uboot-2022.10/include`.

 
- **Symlink issue**
  Dropped `ctype.h` directly into:
  `orangepi/u-boot-orangepi/include`
 
